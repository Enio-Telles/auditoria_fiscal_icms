# Configuração do Banco de Dados
# ================================

# Tipo do banco de dados (postgresql, sqlserver, mysql)
DB_TYPE=postgresql

# Configurações de conexão
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_04565289005297
DB_USER=postgres
DB_PASSWORD=sefin
DB_SCHEMA=dbo

# Configurações de extração
EXTRACTION_CHUNK_SIZE=10000
EXTRACTION_LIMIT=

# Configurações da API Multi-Tenant
API_HOST=127.0.0.1
API_PORT=8003

# Configurações do PostgreSQL central
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=auditoria_central

# Configurações do Ollama (IA)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1

# Configurações de logging
LOG_LEVEL=INFO
LOG_FILE=logs/sistema.log
